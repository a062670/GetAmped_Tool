export default {
  set_vm(state, vm) {
    state.vm = vm;
  },
  set_dm(state, dm) {
    state.dm = dm;
  },
  set_hwnd(state, hwnd) {
    state.hwnd = hwnd;
  },
  set_thread(state, thread) {
    state.thread = thread;
  },
  set_logs(state, logs) {
    state.logs = logs;
  },
};
